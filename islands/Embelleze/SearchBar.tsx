import Component from "$store/components/embelleze/header/SearchBar.tsx";
import type { Props } from "$store/components/embelleze/header/SearchBar.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
